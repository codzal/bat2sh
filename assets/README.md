# assets/

Static artwork used by documentation.

* `logo.jpeg` - project logo, referenced from the root README
  (`README.md`, `README_RU.md`). Keep the filename stable: both readmes
  link it as `assets/logo.jpeg`.
